services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: macromind-ai
    restart: unless-stopped
    ports:
      - "8070:8070"
    environment:
      - NODE_ENV=production
      - PORT=8070
    env_file:
      - .env.local
    volumes:
      - macromind-data:/app/data
    networks:
      - macromind-network

networks:
  macromind-network:
    driver: bridge

volumes:
  macromind-data:
    driver: local
